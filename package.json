{
  "name": "yoshi-monorepo",
  "scripts": {
    "postinstall": "octo bootstrap -n",
    "build": ":",
    "pretest": "octo modules sync --save && octo deps sync --save",
    "test": "octo run test"
  },
  "devDependencies": {
    "octopus-cli": "0.0.8"
  }
}
